# Handoff — 2026-07-03

Continuation of the reproducibility / paper-finishing work. Everything below is committed
and pushed unless flagged "author-side".

## Repo state

**Code repo** (`lya1d_priya_forecast`)
- `main` == `origin/main` == `9094847`; working tree clean.
- Working branch `paper-production` == `main` (fast-forwarded).
- Release tag **`v1.0-paper` → `a84e36d`** (pushed). Tag = the CODE release marker; `main`
  carries all committed artifacts (incl. the seed-band Tier-1 sidecars committed just after the tag).

**Paper repo** (`Knowledge-Distillation-using-PySR-with-PRIYA-suite`, branch `paper-additions`)
- HEAD `9e369f0`, fully pushed. Builds clean (**zero undefined refs**).
- Recent commits: `5ca9474` (log eq:norm/eq:combine + provenance stamps + §7f seed-robustness),
  `0e29a7b` (cross-check fixes), `e013d6b` (sec:3d_preds + tag anchor), `9e369f0` (CHANGES.md).

## Authorship / title / framing  (CORRECTED 2026-07-03 — earlier drafts had this WRONG)

- **First author: Aidan Behmer** (the student). Author order **Behmer, Ho, Avestruz**.
- **Title NOT decided** — README/CITATION.cff use a clearly-marked *working title* only.
- **Student-led research project** — README top + Citation + CITATION.cff say so explicitly, and
  ask people to contact the authors before citing (paper in preparation).
- Earlier README/CFF wrongly listed Ho as first author with a decided title
  ("Knowledge Distillation with PySR on the PRIYA suite") — now removed everywhere.
- NB: "Ho et al. 2025" in the README (line ~64) is a *different, real* paper (the PRIYA MF-GP
  emulator) — leave it.

## What this session did

1. **Committed + pushed the paper** (was uncommitted on disk); added `.gitignore` for LaTeX aux.
2. **5-agent reproducibility cross-check** of every provenance stamp. Verdict: all headline numbers
   reproduce exactly from committed Tier-1 sidecars (9/11 @ z=3.6; 8/9/6 per-z; 5 cross-z; nS
   0.255/0.160/0.379; all 11 taxonomy rows); 5 equation code-refs zero line-drift; figures backed.
   Fixed: 4 STALE comments that swallowed body text via end-of-line `%`; `pareto_A_P.csv`→`pareto_Ap.csv`;
   `plot_seed_band.py`→`paper_figures.plot_seed_band`; Ω_Mh² 0.096 mis-sourced.
   Corrected a false alarm: `a84e36d` is a real 2-parent merge (not a squash) — all git stamps resolve;
   the "unreachable" scare came from a stale local `main` (since fast-forwarded).
3. **4 hardening items**: sec:3d_preds dangling `\ref` fixed; RUN_MANIFEST honest `released:` line;
   **114 seed-band grad_faith sidecars regenerated + committed to `main`** (seed band now Tier-1 /
   no-GP reproducible — validated: `knee_row` on committed sidecars reproduces
   `seed_band_summary.json` to **1.35e-7** across every param/arm); tag `v1.0-paper`.
4. **`CHANGES.md`** (paper repo) — latest-pass delta + cumulative diff vs the original Overleaf
   draft (baseline `1d1dce3`).
5. **Authorship/title/framing fix** (this note's section above).

## How to reproduce (quick)
- Figures/tables Tier-1 (no GP): `PYTHONPATH=src python scripts/make_diagnostic_figs.py` (see `REPRODUCE.md`).
- Whole paper: `notebooks/reproduce_paper.ipynb`.
- Seed band from committed sidecars (no GP): `knee_row` per param over
  `results/paper_production_20260630_perz_sobolev_z2.6-4.2/seed_band/z3.6_seed*/*/refit/z3.6/grad_faith_*.csv`.
- Re-score sidecars (needs GP): `scripts/eval_grad_faithfulness.py` with
  `PYTHONPATH=src:/home/mfho/student_projects/lya_emulator_full`, `--basedir data/kodiaq_gp --log-space`.
- Where each result lives in code: `docs/CODE_REVIEW_MAP.md`.

## Open — author-side (not blocking; all in the paper repo `.tex`)
- **Decide the title** (README/CFF hold only a working title).
- **Confirm author list/order** is complete (currently Behmer, Ho, Avestruz).
- ~40 `\mfho{}` decisions + `\done`/`\additions` review in LaTeX Workshop.
- **Title typo**: "Diagno**i**stics" → "Diagnostics" (inside `\done{}`, left untouched).
- The 9 `% STALE:` flagged value-accuracy numbers (verify-or-cut; superseded by Tab. `multid`/`stats_table`).
- Grep to find them: `grep -nE '\\mfho|% STALE|% code-ref' oja_template.tex`.

## Pointers
- Paper change log: paper repo `CHANGES.md`.
- Code review map: `docs/CODE_REVIEW_MAP.md`.  Reproduce: `REPRODUCE.md`.
- Prior handoffs / internal history: `docs/dev/`.
