# Housecleaning run — synthesis & action plan

**Date:** 2026-06-09 · **Branch:** `stage10-multiz-sobolev` · **Status:** NOT yet committed
**Inputs synthesised:** `audit_lya.md`, `audit_cs.md` (2 audits); `review_lya_reviewer.md`,
`review_cs_reviewer.md`, `review_newcomer_reviewer.md`, `review_consistency_reviewer.md`
(4 reviews); `LEGACY_REMOVAL.md`; `refcheck_shard{0,1,2}.md` (3 citation shards).

---

## Executive summary (8 lines)

1. The repo was rewritten to match its 2026-06-08 pivot: the headline is now a **derivative-faithfulness diagnostic**, not the retired σ_PySR/σ_GP forecast (a forced-Jacobian confound).
2. **Produced:** a new `README.md` (old one backed up to `README_old.md`); two emulator-free notebooks `reproduce_paper_figures.ipynb` + `tutorial_01_explore_diagnostic.ipynb`; this housecleaning doc set.
3. **Removed (staged, not committed):** 3 tracked files — `scripts/compare_eqn_sets.py` (dead stub) and two root `slurm-multid_pysr-*.out` logs; fast suite unchanged at **412 passed / 14 skipped**.
4. **Verdicts:** lya_reviewer PASS, cs_reviewer PASS — both with minor doc nits only. consistency_reviewer and newcomer_reviewer say **CHANGES REQUIRED/REQUESTED**.
5. **One hard blocker (P0):** the diagnostic figures depend on `pareto_*.csv` that are **untracked**, so a fresh clone crashes with `FileNotFoundError`. Verified still live in the working tree.
6. **Science is clean:** every load-bearing number (ns 0.603→0.193, budget@35 0.319 fail, cross-z He II blowup, 0.25 gate, 4-category taxonomy) reproduces from committed sidecars and matches the walkthrough.
7. **Citations:** all 28 bib entries across 3 shards verified REAL and correctly attributed — **zero WRONG, zero UNVERIFIABLE** (incl. the flagged `Sobol1993` / `Owen2003`).
8. **Do before commit:** `git add` the pareto CSVs + `decider_budget_z3.6/` tree (P0), then clear the P1/P2 doc-accuracy items below; the run is otherwise commit-ready.

---

## (1) What was produced

**New / rewritten documentation**
- **`README.md`** — rewritten to lead with the derivative-faithfulness diagnostic and a two-tier
  usage story (emulator-free figure repro vs. full GP/Julia retrain). The previous forecast-era
  README is preserved as **`README_old.md`** (backup confirmed present).
- **This doc set** under `docs/housecleaning/` (audits, reviews, legacy log, citation shards).

**New notebooks (both emulator-free, execute clean, exit 0, zero GPy/PySR/Julia imports)**
- **`notebooks/reproduce_paper_figures.ipynb`** — the paper reproducer; regenerates all four
  diagnostic figures from committed sidecars (the 4th, `crossz_faithfulness`, is generated by the
  notebook itself, not by the script).
- **`notebooks/tutorial_01_explore_diagnostic.ipynb`** — the diagnostic tour (Mirage-in-one-table,
  taxonomy, budget control).
- The three old forecast-era notebooks (`01_gp_only`, `02_forecast_only`, `03_refit_and_forecast`)
  were **kept** (still valid for the GP/forecast API, but predate the pivot).

**What was removed (staged via `git rm`, NOT committed — confirmed still staged)**
- `scripts/compare_eqn_sets.py` — unimplemented phase-7 stub (`raise SystemExit`), zero importers.
- `slurm-multid_pysr-49350027.out`, `slurm-multid_pysr-49355289.out` — root SLURM logs that
  violated the repo's own `slurm-*.out` ignore rule, no possible importer.
- Fast suite after removal: **412 passed / 14 skipped** — identical to baseline; nothing reverted.
- Everything medium/low confidence (the `pysr_hypothesis` and `refit_residual` clusters, Phase 1.5/2
  `results/` trees, `README_v2.md` / `LOCAL_PAPER_HANDOFF.md`, era-stale design docs) was **deferred
  to the human** as PROPOSED-FOR-HUMAN, correctly — those have live test importers or sit under the
  protected `results/` tree. See `LEGACY_REMOVAL.md` for the full 18-row deferral table.

---

## (2) Review verdicts — did README/notebooks pass?

| Reviewer | Verdict | Bottom line |
|---|---|---|
| **lya_reviewer** (science) | **PASS** (2 minor) | Every load-bearing number in README + both notebooks matches the walkthrough and committed sidecars. No wrong/overstated claim. |
| **cs_reviewer** (RSE) | **PASS** (minor doc) | All README commands run as written; both new notebooks valid nbformat 4.5 + execute clean; legacy cleanup SAFE. |
| **newcomer_reviewer** (fresh clone) | **CHANGES REQUIRED** | A fresh clone **cannot** reproduce a figure — the `pareto_*.csv` are untracked (P0 blocker). |
| **consistency_reviewer** (cross-doc) | **CHANGES REQUESTED** | Science direction consistent everywhere; but figure-name/figure-count/test-count contradictions across docs. |

**Net:** the README and notebooks are scientifically sound and run in *this* working tree, but a
fresh clone is broken by the P0 data-tracking gap, and several doc-accuracy contradictions remain.
**Not commit-ready until the P0 + the P1/P2 doc items below are cleared.**

### Open issues to fix before commit (deduplicated across the 4 reviews)

**P0 — BLOCKER (newcomer_reviewer; verified live this run)**
- `pareto_*.csv` in the three z=3.6 repro dirs are on disk but **untracked** (not gitignored — just
  never `git add`ed). `pareto_diag.py:31` reads them with no existence guard, so
  `make_diagnostic_figs.py` and `reproduce_paper_figures.ipynb` both crash with `FileNotFoundError`
  on a fresh clone. The whole `results/decider_budget_z3.6/` tree is untracked (only 1 of its files
  is even tracked). **Fix is one `git add` (see TODO).**

**P1 — fresh-clone discoverability & stale embeds**
- README "Where to start" lists only the OLD `01/02/03` notebooks; never points at the two NEW
  emulator-free notebooks (newcomer + consistency).
- **Jupyter is not installable from the documented steps** — no `jupyterlab`/`nbconvert` in
  `requirements.lock.txt` or `pyproject.toml`; README never says how to launch a notebook (newcomer).
- Walkthrough embeds `summary_scorecard.png` + `ns_money_panel.png`, but the reproducer now emits
  `faithfulness_scorecard.png` + `ns_budget_panel.png`; re-running leaves the walkthrough images
  stale. Repoint the two `![]()` links and `git rm` the two orphans (consistency).

**P2 — doc-accuracy contradictions**
- README says reproducer makes "three" figures (script prints "3"), but the paper/walkthrough/notebook
  turn on **four** (the 4th, cross-z, is notebook-only). State this, or add cross-z to the script
  (consistency).
- `reproduce_paper_figures.ipynb` writes to the committed `results/single_z_stage_pareto_diag/`,
  dirtying 8 tracked figures on execution; repoint `OUT` to a scratch dir like `tutorial_01` does
  (lya_reviewer).
- README/notebook prose says the reproducer "reads the sidecars in
  `results/single_z_stage_pareto_diag/`" — that dir holds only OUTPUT figures; real inputs are
  `single_z_stage{6_log,9}/refit/z3.6/` + `decider_budget_z3.6/refit/z3.6/` (cs_reviewer).
- Repro notebook pins an unregistered kernel `priya-forecast-venv`; tutorial uses generic `python3`
  — inconsistent. Repin to `python3` or document `ipykernel install` (newcomer).

**P3 — wording / hygiene (non-blocking)**
- README/HANDOFF call the gated GP test a numpy<2/GPy "**error**" — it actually **skips** cleanly
  (`lyaemu` absent). Run is `412 passed, 14 skipped, 0 errors` (all 4 reviewers flag this).
- `HANDOFF.md` says "~412 pass, **13 skip**" (actual 14) and "**26 commits** ahead" (actual 27)
  (consistency).
- README understates the data contract — also needs `pareto_*.csv`, not just `grad_faith_*.csv`
  (newcomer P3).
- Build-scratch generators `notebooks/_build_*.py` sit next to the shipped notebooks; move or
  gitignore (newcomer P3).

---

## (3) Citation audit result

Source: `oja_template.bib` (28 entries), sharded 3 ways by `index mod 3`, each shard verified by
WebSearch/WebFetch against ADS / arXiv / publisher, plus special attention to the two recently-added
stats refs.

**Result: every entry checked is REAL and correctly attributed. ZERO entries flagged WRONG. ZERO
flagged UNVERIFIABLE.**

No fabricated, mis-attributed, mis-authored, or wrong-venue entry was found in any shard. The only
blemishes are cosmetic, **not** errors:

| bibkey | "flag" | reality / correction |
|---|---|---|
| `Jin_2024` | year-convention nit | DOI 10.1093/mnras/stae2741, vol 536(3):2277-2293 correct. `year=2024` = arXiv/advance-access year; OUP issue is Jan 2025. Bump to 2025 only if strict ADS-issue-year consistency is wanted — cosmetic, not WRONG. |
| `Ma_2025` | year-convention nit | DOI 10.1093/mnras/staf2262 correct. `year=2025` = arXiv year; ADS issue is 2026 (`2026MNRAS.546f2262M`). Cosmetic. |
| `Hernquist` | missing page | ApJ 457:L51 (Letter, L51-L55), DOI 10.1086/309899 resolves. Bib omits the `pages` field — incompleteness, not an error. |
| `Sobol1993` (flagged) | apparent vol/issue mismatch | **Correct as written.** MMCE **1(4):407-414, 1993** is the canonical form (Saltelli textbooks). Third-party DBs (SciRP) mislabel it "vol 4" by collapsing issue→volume; the bib is the right variant. |
| `Owen2003` (flagged) | — | Statistica Sinica 13(1):1-17, 2003 — verified verbatim against the publisher PDF. Correct (issue number optional). |

**No citation corrections are required to commit.** If you want strict ADS-issue-year consistency,
optionally bump `Jin_2024`→2025 and `Ma_2025`→2026 and add the `Hernquist` page range — all cosmetic.

---

## (4) Prioritised TODO before committing

**MUST do (blocks a fresh-clone reproduction):**
1. **[P0] Track the pareto CSVs + the budget tree** so a fresh clone can build the figures:
   ```bash
   git add results/single_z_stage6_log/refit/z3.6/pareto_*.csv \
           results/single_z_stage9/refit/z3.6/pareto_*.csv \
           results/decider_budget_z3.6/   # whole untracked tree (pareto_ns.csv + grad_faith_ns.csv)
   ```
   Optionally also guard `pareto_diag.py:31` so a missing pareto file skips that series instead of
   crashing.

**SHOULD do (doc accuracy; reviewers gate on these):**
2. **[P1]** Add `reproduce_paper_figures.ipynb` + `tutorial_01_explore_diagnostic.ipynb` to README
   "Where to start" (flag emulator-free); add the same pointer to `HANDOFF.md`.
3. **[P1]** Add a Jupyter install + kernel-registration line to the README notebook section
   (`pip install jupyterlab` and `python -m ipykernel install --user --name priya-forecast-venv ...`),
   since neither lockfile nor pyproject ships Jupyter.
4. **[P1]** Repoint the two walkthrough image links to `faithfulness_scorecard.png` /
   `ns_budget_panel.png`, then `git rm` the orphaned `summary_scorecard.png` + `ns_money_panel.png`.
5. **[P2]** Reconcile the figure count: state in README that the script makes 3 figures and the 4th
   (cross-z) comes from the notebook — or add cross-z to `make_diagnostic_figs.py` so they agree.
6. **[P2]** Point `reproduce_paper_figures.ipynb`'s `OUT` at a scratch dir (e.g.
   `results/_repro_scratch`) so running it stops dirtying 8 committed figures.
7. **[P2]** Fix the README/notebook prose that calls `single_z_stage_pareto_diag/` the sidecar
   *source* (it is the output dir); name the real input dirs and add `pareto_*.csv` to the contract.
8. **[P2]** Repin the repro notebook kernel to `python3` (matching the tutorial) or document the
   `ipykernel install`.

**NICE to do (wording / hygiene; non-blocking):**
9. **[P3]** Globally change the gated-GP-test "error" wording to "skips when `lyaemu` is absent";
   correct `HANDOFF.md` "13 skip"→"14 skip" and "26 commits"→"27".
10. **[P3]** Move/gitignore `notebooks/_build_*.py`; consider the `LEGACY_REMOVAL` PROPOSED-FOR-HUMAN
    items (`.claude/` lock+nfs turd, `outputs/` 126 MB scratch, Phase 1.5/2 `results/` archive,
    `README_v2.md` consolidation) as a separate cleanup pass.
11. **[P3, optional]** Cosmetic citation year bumps (`Jin_2024`→2025, `Ma_2025`→2026) +
    `Hernquist` page range, only if strict ADS-issue-year consistency is desired.

**Commit sequencing suggestion:** do TODO #1 first (it is the only correctness blocker), then the
P1/P2 doc fixes, run `PYTHONPATH=src pytest tests/ -q -k "not slow"` once more to confirm 412/14, then
commit the staged removals + README + notebooks together. Handle the `.claude/`/`results/`-archive
hygiene as a separate follow-up commit.
