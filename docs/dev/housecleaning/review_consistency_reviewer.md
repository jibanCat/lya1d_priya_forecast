# Consistency review — README / HANDOFF / walkthrough / notebooks

**Reviewer:** consistency_reviewer
**Date:** 2026-06-09
**Scope:** cross-document agreement on commands, paths, numbers, and science
direction across `README.md`, `HANDOFF.md`,
`docs/PARETO_FAITHFULNESS_WALKTHROUGH.md`, the two new emulator-free notebooks
(`notebooks/reproduce_paper_figures.ipynb`,
`notebooks/tutorial_01_explore_diagnostic.ipynb`), and
`docs/housecleaning/LEGACY_REMOVAL.md`.

## Verdict: **CHANGES REQUESTED**

The science direction is consistent everywhere (PySR distillation → derivative-
faithfulness diagnostic; σ-forecast superseded; Sobolev cure; gate 0.25). All
referenced result paths, configs, the CLI entry point, the lockfile versions, the
LEGACY_REMOVAL removals, and both notebooks verify against the repo, and **both
notebooks execute cleanly emulator-free**. The cross-z numbers printed by the
repro notebook match the walkthrough table to 3 d.p. However there are several
**factual contradictions on figure names, figure count, and test/skip counts**
that will mislead a reader running the documented commands. None is a science
error; all are doc-accuracy bugs. Fix the P1/P2 items before merge.

---

## Prioritised issue list

### P1 — Walkthrough embeds figure names the canonical reproducer no longer emits
`docs/PARETO_FAITHFULNESS_WALKTHROUGH.md` embeds:
- line 81: `![Scorecard](../results/single_z_stage_pareto_diag/summary_scorecard.png)`
- line 86: `![ns money panel](../results/single_z_stage_pareto_diag/ns_money_panel.png)`

But `scripts/make_diagnostic_figs.py` (the reproducer README/HANDOFF point to)
emits `faithfulness_scorecard.{png,pdf}` and `ns_budget_panel.{png,pdf}`. The
`summary_scorecard.png` / `ns_money_panel.png` files are stale Jun-8 copies still
git-tracked; re-running the reproducer does **not** refresh them, so the
walkthrough's two embedded images silently go stale.
**Repro:** `PYTHONPATH=src python scripts/make_diagnostic_figs.py --out-dir results/single_z_stage_pareto_diag`
then `git status` — `summary_scorecard.png`/`ns_money_panel.png` are untouched
while `faithfulness_scorecard.png`/`ns_budget_panel.png` are rewritten.
**Fix:** repoint the two walkthrough `![]()` links to `faithfulness_scorecard.png`
and `ns_budget_panel.png`, and `git rm` the orphaned `summary_scorecard.png` +
`ns_money_panel.png`.

### P2 — README says the reproducer makes "three" figures; the paper turns on four
- `README.md:117`: "rebuilds the **three** diagnostic figures".
- `scripts/make_diagnostic_figs.py:156` literally prints "wrote 3 figures".
- But the repro notebook (`reproduce_paper_figures.ipynb`, md cell 0/17) and the
  walkthrough both present **four** figures — the 4th, `crossz_faithfulness`, is
  generated **only by the notebook**, never by `make_diagnostic_figs.py`.

So a reader who runs the README command gets 3 figures and cannot regenerate
`crossz_faithfulness` (which the walkthrough embeds at line 230) without the
notebook. **Fix:** in README, state that the script makes 3 figures and the 4th
(cross-z) is regenerated by `notebooks/reproduce_paper_figures.ipynb`; OR add the
crossz figure to `make_diagnostic_figs.py` so script and notebook agree.

### P2 — README/HANDOFF never mention the two new headline notebooks
README "Where to start" (lines 144-151) and the layout block (line 168) list only
the **old** emulator-required `01_gp_only` / `02_forecast_only` /
`03_refit_and_forecast` notebooks. The two NEW emulator-free notebooks that are
the housecleaning deliverable — `reproduce_paper_figures.ipynb` (the paper
reproducer) and `tutorial_01_explore_diagnostic.ipynb` (the diagnostic tour) — are
mentioned in **neither README nor HANDOFF**. A new reader is pointed away from the
current, dependency-free entry points toward the superseded ones.
**Fix:** add both new notebooks to README "Where to start" (flag them as
emulator-free) and to HANDOFF "How to run".

### P3 — Test/skip counts disagree across docs; the "GPy env error" is actually a skip
Measured (`PYTHONPATH=src .venv/bin/python -m pytest tests/ -q -k "not slow"`):
**412 passed, 14 skipped, 0 errors.**
- `LEGACY_REMOVAL.md`: "412 passed, 14 skipped" — **correct**.
- `HANDOFF.md:69`: "~412 pass, **13 skip**" — **wrong** (14 skip).
- `README.md:111-112` and `HANDOFF.md:70`: both assert "1 pre-existing
  numpy<2/GPy environment **error** on `test_real_gp_predicts_at_fiducial`". In
  this environment that test **skips cleanly** ("could not import 'lyaemu'"), it
  does not error; there are 0 errors and it is one of the 14 skips.
**Fix:** change HANDOFF "13 skip" → "14 skip", and reword the
`test_real_gp_predicts_at_fiducial` line in both README and HANDOFF from "error"
to "skips when the emulator/lyaemu is absent" (or qualify "errors only when lyaemu
imports but GPy fails under numpy 2.x").

### P3 — HANDOFF commit count stale
`HANDOFF.md:4`: "26 commits ahead of `main`". Actual: `git rev-list --count
main..stage10-multiz-sobolev` = **27**. PR-open claim is correct (PR #6, OPEN).
**Fix:** update to 27 (or make it version-agnostic).

---

## What is consistent (verified, no action)
- Science direction identical in all four docs: diagnostic headline, σ-forecast
  superseded/confounded, Sobolev cure, gate **0.25** (`GATE_TOL` confirmed 0.25).
- ns Mirage numbers agree: walkthrough 0.603→0.193, budget@35 0.319; HANDOFF
  0.60→0.19; tutorial 0.60→0.19; repro notebook prints ns 0.211/0.193/0.211.
- Cross-z table printed by `reproduce_paper_figures.ipynb` matches walkthrough
  exactly (herei 0.186/0.060/0.709, heref 0.299/0.206/2.690, alphaq
  0.097/0.173/1.556).
- Both notebooks: valid nbformat-4.5, execute cleanly emulator-free (no
  GPy/PySR/Julia imported). repro=19 cells (8 md/11 code, kernel
  `priya-forecast-venv`, registered); tutorial=32 cells. All referenced sidecar
  dirs exist (`single_z_stage6_log`, `single_z_stage9`, `decider_budget_z3.6`,
  `single_z_z{2.6,4.2}_{value,sobolev}`).
- Install facts in README match `requirements.lock.txt` / `pyproject.toml`: numpy
  1.26.4, GPy 1.13.2, pysr 1.5.10, pandas 2.3.3; numpy<2 / pandas<3 caps; CLI
  `priya-forecast = priya_forecast.cli:main` imports.
- Env vars (`PYTHON_JULIAPKG_PROJECT=$HOME/.julia_env`,
  `JULIA_DEPOT_PATH=$HOME/.julia`, `PYTHONPATH=src:/home/mfho/student_projects/lya_emulator_full`)
  identical in README and HANDOFF.
- LEGACY_REMOVAL fully verified: 3 staged deletions
  (`compare_eqn_sets.py`, two root `slurm-multid_pysr-*.out`), zero-importer claim
  holds, baseline 412/14 matches, `README_old.md` backup present.

> Note: my first nbclient run of `reproduce_paper_figures.ipynb` tripped the final
> assert because `crossz_faithfulness.pdf` was absent in a stale working tree at
> start; a clean re-run wrote both PDF+PNG and passed. The notebook itself is
> correct and reproducible — not a notebook bug, but it shows the assert depends on
> a clean working tree.
