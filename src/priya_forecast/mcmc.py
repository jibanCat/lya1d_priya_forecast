"""emcee-driven MCMC for the single-z eBOSS P1D forecast.

Phase 4 will fill this out. EnsembleSampler with 4*ndim walkers, 20% burn-in,
HDFBackend storage, and an autocorr-based convergence warning.
"""

# TODO(phase4): run_mcmc(likelihood, theta_fid, params, n_steps, backend_path)
