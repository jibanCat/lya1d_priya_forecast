"""YAML config loader + dataclass-based validation.

Phase 1 will fill this out. Loads `configs/*.yaml` and `configs/eqns/*.yaml`,
validates required fields, and produces typed config objects consumed by
the rest of the pipeline.
"""

# TODO(phase1): RunConfig, EqnConfig, HPOConfig dataclasses + load_yaml helpers
